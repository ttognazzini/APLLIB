create or replace function APLLIB/ISNUMERIC (INPUT VARCHAR(1024))
returns DECIMAL(1,0)
LANGUAGE SQL
CONTAINS SQL
DETERMINISTIC
return CASE WHEN NOT REGEXP_LIKE(INPUT , '^\d*\ *$') THEN 0  ELSE 1 END;
